# kiwik-project (GitHub Pages) 검사 보고서

검사 주소: https://icerence.github.io/kiwik-project/
검사 기준: 모바일 (기본값) — Lighthouse는 데스크톱도 추가 측정
검사일: 2026-08-11

## 한눈에 보기

| 항목 | 결과 | 판정 |
| --- | --- | --- |
| HTML 문법 | 오류 5건, 경고 0건 | 고칠 것 있음 (원인 단일) |
| CSS 문법 | 오류 30건, 경고 72건 | 고칠 것 있음 (전부 외부 라이브러리) |
| JS 린트 | 오류 508건, 경고 1,135건 | 자체 코드는 통과, 외부 라이브러리가 대부분 |
| 성능 | 56점 | 나쁨 |
| 접근성 | 100점 | 좋음 (자동 검사 기준) |
| 권장 사항 | 100점 | 좋음 |
| 검색 최적화 | 100점 | 좋음 |

가장 먼저 볼 것: HTML 오류 5건은 전부 Tailwind CSS v4 CDN 문법(`@theme`, `@utility`)을 W3C 검사기가 모르는 문법으로 인식해서 난 것이고, CSS 오류 30건은 전부 `swiper` CDN 파일에서 났다. 직접 만든 JS(`main.js`)는 오류 0건, 경고 47건(전부 `prefer-const`)뿐이다. **성능 점수는 측정 기준에 따라 크게 다르다** — 모바일 56점(나쁨)인데 데스크톱은 99점(좋음)이다. 아래 표는 모바일 기준이며, 데스크톱은 4-2절에 따로 적었다.

### 데스크톱 기준 Lighthouse (참고)

| 항목 | 결과 | 판정 |
| --- | --- | --- |
| 성능 | 99점 | 좋음 |
| 접근성 | 96점 | 개선 필요 (색 대비 1건) |
| 권장 사항 | 100점 | 좋음 |
| 검색 최적화 | 100점 | 좋음 |

## 1 — HTML 문법

### 오류 5건

| 줄 | 무엇이 잘못됐는가 | 어떻게 고치는가 |
| --- | --- | --- |
| 20:31 | `style` 태그의 `type` 값은 `text/css`만 허용됨. ("The only allowed value for the type attribute for the style element is text/css") | Tailwind Play CDN이 `@theme` 토큰을 읽으려면 `type="text/tailwindcss"`가 필요해서 나는 오류다. 표준 문법 위반이지만 CDN 방식을 쓰는 한 지울 수 없다 |
| 21 | `@theme` at-rule을 모름. ("CSS: Unrecognized at-rule "@theme"") | 위와 같은 원인. Tailwind v4 전용 문법 |
| 149 | `@theme static` at-rule을 모름 | 위와 같은 원인 |
| 176 | `@utility grid-...` at-rule을 모름 | 위와 같은 원인 |
| 180 | `@utility grid-...` at-rule을 모름 | 위와 같은 원인 |

### 경고 0건

이 5건은 서로 다른 버그가 아니라 **같은 원인 하나**(Tailwind Play CDN + `@theme`/`@utility` 문법)에서 나온다. `design-system-harness.md`에서 이미 CDN 방식을 채택하기로 정했다면, W3C 표준 검사기 기준으로는 "오류"로 남는 게 정상이며 실제 화면 동작과는 무관하다. 이 오류를 0건으로 만들려면 Tailwind를 빌드 단계(PostCSS)로 옮겨 `<style>` 태그 대신 컴파일된 `.css` 파일을 링크해야 하는데, 이는 이 프로젝트의 "빌드 없는 정적 사이트" 원칙과 충돌한다.

## 2 — CSS 문법

| 줄 | 무엇이 잘못됐는가 | 어떻게 고치는가 |
| --- | --- | --- |
| swiper-bundle.min.css:13 외 다수 | 구문이 깨짐 (Parse Error). `.swiper-3d`, `.swiper-css-mode` 등 최신 CSS 문법을 W3C 검사기가 못 읽음 | 직접 만든 코드가 아니다. `https://cdn.jsdelivr.net/npm/swiper@12/swiper-bundle.min.css`(외부 CDN)에서 난 오류로, 수정 대상이 아니다 |

경고 72건도 같은 CDN 파일에서 난 것으로 추정된다(검사기 설정상 경고는 건수만 받고 개별 내용은 받지 않았다). 자체 CSS(`assets/css/*.css`, 인라인 `@theme` 블록)에서 난 CSS 오류는 없다.

## 3 — JavaScript 린트

검사 세트: ESLint recommended

| 파일 | 오류 | 경고 | 구분 |
| --- | --- | --- | --- |
| `main.js` (자체 코드) | 0건 | 47건 (`prefer-const`) | 직접 만든 코드 |
| Tailwind Play CDN 런타임 | 65건 | 1,084건 | 외부 라이브러리 |
| `gsap.min.js` | 242건 | 0건 | 외부 라이브러리(CDN) |
| `ScrollTrigger.min.js` | 128건 | 0건 | 외부 라이브러리(CDN) |
| `ScrollToPlugin.min.js` | 12건 | 0건 | 외부 라이브러리(CDN) |
| `swiper-bundle.min.js` | 61건 | 4건 | 외부 라이브러리(CDN) |

외부 라이브러리 5개에서 난 오류 508건은 전부 `.min.js`이며 직접 만든 코드가 아니므로 수정 대상이 아니다.

### `main.js` 경고 47건 — 전부 `prefer-const`

| 줄 | 규칙 | 내용 | 고치는 이유 |
| --- | --- | --- | --- |
| 24, 25, 27, 28, 35, 43, 47, 59, 67, 73 외 37건 | `prefer-const` | `let`으로 선언했는데 이후 재할당이 한 번도 없는 변수 (`reduceMotionQuery`, `desktopQuery`, `panels`, `footer`, `previous`, `moved`, `dotLinks`, `el`, `utilityBar`, `shouldShow` 등) | `const`가 "다시 대입하지 않는다"는 의도를 더 정확히 드러내고, 실수로 재할당하는 사고를 막는다 |

같은 유형 47건을 위 표 10건으로 줄였다. 나머지 37건도 `let` → `const`로 바꾸면 전부 해결된다.

## 4 — 성능

측정 기준: 모바일
측정 도구: 로컬 `lighthouse` CLI 13.4.1 (PSI API는 `HTTP 429`로 실패해 로컬 CLI로 재시도)

| 지표 | 측정값 | 비중 | 점수 |
| --- | --- | --- | --- |
| 총 차단 시간 | 10 ms | 30% | 1.00 (좋음) |
| 최대 콘텐츠 표시 시간 | 11.0 s | 25% | 0.00 (나쁨) |
| 누적 레이아웃 이동 | 0 | 25% | 1.00 (좋음) |
| 첫 콘텐츠 표시 시간 | 10.0 s | 10% | 0.00 (나쁨) |
| 속도 지수 | 10.0 s | 10% | 0.09 (나쁨) |

가중치가 가장 큰 두 지표(총 차단 시간 30%, 최대 콘텐츠 표시 시간 25%) 중 최대 콘텐츠 표시 시간이 최저점이라 전체 점수(56점)를 크게 깎았다. "화면이 뜨는 속도" 자체가 느린 것이지, 클릭 반응(총 차단 시간)이나 레이아웃 밀림(누적 레이아웃 이동)은 문제없다.

### 원인으로 지목된 감사 항목

| 항목 | 내용 |
| --- | --- |
| `render-blocking-insight` | 화면 표시를 막는 파일이 있음 — 예상 절감 7,750 ms |
| `cache-insight` | 캐시 유효 기간이 짧음 — 예상 절감 1,653 KiB |
| `unused-javascript` | 쓰지 않는 JS가 있음 — 예상 절감 21 KiB |
| `network-dependency-tree-insight` | 요청이 사슬처럼 이어져 있음(critical request chain) |
| `forced-reflow-insight` | JS가 스타일 계산 직후 레이아웃 값을 읽어 강제 리플로우 발생 |

렌더링을 막는 리소스(외부 CDN 스크립트·CSS를 head에서 동기 로드하는 것으로 추정) 정리가 가장 큰 지렛대다.

### 4-2 — 데스크톱 기준 (참고 측정)

측정 도구: 로컬 `lighthouse` CLI 13.4.1

| 지표 | 측정값 | 점수 |
| --- | --- | --- |
| 총 차단 시간 | 0 ms | 1.00 |
| 최대 콘텐츠 표시 시간 | 0.8 s | 0.98 |
| 누적 레이아웃 이동 | 0 | 1.00 |
| 첫 콘텐츠 표시 시간 | 0.7 s | 0.97 |
| 속도 지수 | 1.0 s | 0.96 |

**성능 99점(좋음).** 모바일 56점과 이렇게 차이가 나는 이유는 Lighthouse가 모바일 기준에서는 느린 회선(시뮬레이션한 4G)과 저사양 CPU 조건을 적용하기 때문이다. 데스크톱 조건에서는 같은 페이지가 1초 안에 다 뜬다. 즉 페이지 자체의 결함이라기보다 "모바일 회선 조건에서 무거운 외부 리소스(CDN JS·CSS)를 순차 로드하는 구조"가 병목이라는 뜻이다. 실사용자 다수가 모바일이라면 모바일 점수(56점)를 기준으로 4-1절의 개선을 우선한다.

같은 도구 실행에서 걸린 감사 항목 중 모바일과 다른 것은 `color-contrast`(색 대비 부족) 하나뿐이며, 이는 접근성 점수에 반영됐다(아래 5절).

## 5 — 접근성

**모바일 100점 / 데스크톱 96점.** 모바일 기준은 자동 검사 항목을 모두 통과했다. 데스크톱 기준에서는 `color-contrast`(글자와 배경의 대비가 낮음) 1건이 걸렸다 — 화면 레이아웃이 바뀌면서 데스크톱 너비에서만 드러나는 글자/배경 조합으로 보이며, 글자색이나 배경색의 밝기 차이를 키우면 해결된다. 정확한 위치는 브라우저 개발자 도구의 Lighthouse 패널이나 axe DevTools로 확인해야 한다(이 CLI 출력에는 대상 요소의 셀렉터가 포함되지 않았다).

axe-core 기준 자동 판정만 반영된 점수이므로 "누구나 쓸 수 있다"로 확대 해석하지 않는다.

자동 검사로는 아래 세 가지가 확인되지 않는다. 직접 해 보고 판단한다.

1. 키보드 `Tab` 키만으로 화면 전체를 쓸 수 있는가
2. 포커스가 한 곳에 갇히지 않는가
3. 눈에 보이는 순서와 코드 순서가 맞는가

## 6 — 검색 최적화와 권장 사항

**검색 최적화 100점, 권장 사항 100점.** 두 항목 모두 자동 검사에서 걸린 것이 없다.

## 검사 도구와 한계

이 보고서는 W3C Nu HTML Checker, W3C CSS Validator, ESLint(recommended), 로컬 Lighthouse CLI 13.4.1 결과를 옮긴 것이다. 문법 검사는 코드가 규칙에 맞는지만 보므로, 문법이 맞아도 화면이 의도와 다르게 보일 수 있다. 성능 점수는 잴 때마다 몇 점씩 달라지므로(인터넷 속도·서버 상태 영향) 이번 56점은 한 번의 측정값이다. 판단이 필요하면 3회 재측정해 비슷하게 나오는 값을 쓴다. 자동 검사로는 키보드 이동, 포커스 순서, 화면 낭독기 동작을 확인할 수 없다 — 사람이 직접 확인해야 한다.

## 7 — 먼저 할 일

1. **성능(56점)을 끌어올린다.** head에서 동기 로드 중인 CDN 스크립트/CSS(렌더링 차단 7,750 ms 절감 여지)부터 `defer`나 지연 로드로 정리한다. 최대 콘텐츠 표시 시간(11.0s)·첫 콘텐츠 표시 시간(10.0s)이 낮은 원인이다.
2. **`assets/js/main.js`의 `let`을 `const`로 바꾼다 (47건).** 자체 코드에서 유일하게 실제로 고칠 수 있는 문법 항목이며, 동작에는 영향이 없다.
3. HTML/CSS 오류(35건)는 전부 Tailwind CDN 문법과 `swiper` 외부 CDN에서 난 것으로, 현재 "빌드 없는 CDN" 구조를 유지하는 한 고칠 대상이 아니다. 그대로 둘지, 빌드 단계를 도입할지는 결정이 필요하면 별도로 논의한다.

참고: 데스크톱 기준에서만 걸린 `color-contrast`(색 대비 부족) 1건은 위 목록에 넣지 않았다. 위 세 가지보다 우선순위가 낮고, 정확한 위치를 브라우저 도구로 먼저 확인해야 고칠 수 있다.

## 부록 — 이번 검사에서 바뀐 환경

성능 재검사를 위해 로컬에 `lighthouse` CLI(13.4.1)를 전역 설치했다(`npm i -g lighthouse`). PageSpeed Insights API는 일일 쿼터가 소진돼(`HTTP 429`) 계속 실패하므로, 이후에도 이 컴퓨터에서는 로컬 CLI 경로로 자동 재시도된다.
